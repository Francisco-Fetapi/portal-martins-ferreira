export interface IPost {
  id: string;
  title: string;
  content: string;
  photo: string;
  author: string;
  // isMine:boolean;
  // saved:boolean;
  // liked:boolean;
  // disliked:boolean;
  // approved:boolean;
  // likes:number;
  // dislikes:number;

  created_at: string;
}
